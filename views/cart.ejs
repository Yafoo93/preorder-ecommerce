<h2>Your Cart</h2>
<% if (products.length === 0) { %>
  <p>Your cart is empty.</p>
<% } else { %>
  <ul>
    <% products.forEach(p => { %>
      <li><strong><%= p.name %></strong> - $<%= p.price %></li>
    <% }) %>
  </ul>
<% } %>
